name: blm2-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  - pip
  - numpy
  - pandas
  - matplotlib
  - tqdm
  - seaborn
  - scipy

# conda env create --file conda-env.yml
# conda env remove --name blm2-env